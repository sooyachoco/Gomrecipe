{
  "name": "gomjiyeon-recipe-note",
  "private": true,
  "version": "1.0.0",
  "description": "곰지연을 위한 읽기 공개 / 관리자 편집 레시피 노트",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
